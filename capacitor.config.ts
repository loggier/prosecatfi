import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prosecatfi.app',
  appName: 'ProPersonnel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
