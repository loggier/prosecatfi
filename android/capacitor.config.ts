import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prosecatfi.app',
  appName: 'Prosecatfi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
